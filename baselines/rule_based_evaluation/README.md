# Evaluation Rule-based Algorithm

## Test Set Construction:

Adjust the `test_set_selection.py` for your needs and output the test files in needed setting. You can download the original data via instructions in the main `README.md` in this project.

## Evaluation

Adjust the main method in `algorithm_eval.py` and run this script to output a evaluation txt file and confusion matrices of root and quality.